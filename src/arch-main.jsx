import React from 'react'
import ReactDOM from 'react-dom/client'
import ArchPage from './ArchPage.jsx'
import './arch.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ArchPage />
  </React.StrictMode>,
)
